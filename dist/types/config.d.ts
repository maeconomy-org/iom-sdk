/**
 * Configuration types for the new SDK architecture
 */
import { SDKConfig, ServiceConfig, ErrorHandlingConfig, SDKError } from '../config';
export type { SDKConfig, ServiceConfig, ErrorHandlingConfig, SDKError };
